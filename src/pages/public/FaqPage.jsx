import { ChevronDown } from 'lucide-react'
import { useState } from 'react'
import { PageContainer } from '../../components/common/PageContainer'
import { PageHeader } from '../../components/public/PageHeader'
import { useSeo } from '../../hooks/useSeo'
const faqs=[['How do I book?','Search for a property, open its details, select a room and continue through the booking flow when booking is enabled.'],['How do I cancel?','Cancellation options depend on the property policy shown before confirmation. Contact support when assistance is needed.'],['How do I become a property owner?','Create an account as a guest and contact DONROOM. An administrator reviews and promotes approved accounts to Property Owner.'],['Are bookings secure?','DONROOM uses Firebase Authentication and protected data access. Payment and final booking features are introduced in the dedicated booking phase.'],['How do I contact property owners?','Verified contact and messaging options are shown as part of confirmed booking communication.']]
export default function FaqPage(){useSeo({title:'FAQ',description:'Answers to common DONROOM booking and property owner questions.',path:'/faq'}); const [open,setOpen]=useState(0); return <><PageHeader eyebrow="Helpful answers" title="Frequently asked questions" description="Everything you need to know before finding or listing a stay."/><PageContainer className="max-w-4xl py-14">{faqs.map(([q,a],i)=><div className="mb-3 overflow-hidden rounded-2xl bg-white shadow-sm" key={q}><button aria-expanded={open===i} className="flex w-full items-center justify-between gap-4 p-5 text-left font-black" onClick={()=>setOpen(open===i?-1:i)}><span>{q}</span><ChevronDown className={`transition ${open===i?'rotate-180':''}`}/></button>{open===i&&<p className="px-5 pb-5 leading-7 text-muted">{a}</p>}</div>)}</PageContainer></>}
