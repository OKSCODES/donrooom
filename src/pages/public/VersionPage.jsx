import { VERSION_INFO } from '../../constants/version'
import { useSeo } from '../../hooks/useSeo'
export default function VersionPage(){useSeo({title:'Version Information | DONROOM',description:'Current DONROOM application version and release channel.'});return <main id="main-content" className="mx-auto max-w-3xl px-4 py-20"><section className="rounded-[2rem] bg-white p-8 shadow-xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-donroom-success">Release information</p><h1 className="mt-3 text-4xl font-black">DONROOM {VERSION_INFO.version}</h1><dl className="mt-8 divide-y divide-black/10">{[['Build number',VERSION_INFO.buildNumber],['Release date',VERSION_INFO.releaseDate],['Channel',VERSION_INFO.channel]].map(([key,value])=><div key={key} className="flex justify-between py-4"><dt className="font-bold">{key}</dt><dd>{value}</dd></div>)}</dl><a href="/CHANGELOG.md" className="mt-6 inline-flex rounded-xl bg-black px-5 py-3 font-bold text-white">View changelog</a></section></main>}
