import { useEffect, useState } from 'react'; import toast from 'react-hot-toast'; import { PropertyPageHeader } from '../../components/property/PropertyPageHeader'; import { usePropertyData } from '../../hooks/usePropertyData'; import { saveOwnerProperty } from '../../services/propertyService'
const initial={notifications:{booking:true,review:true,cancellation:true},emailPreferences:true,whatsappPreferences:true,businessHours:{open:'08:00',close:'20:00'},holidays:[]}
export default function PropertySettingsPage(){const {user,property,refresh}=usePropertyData();const [form,setForm]=useState(initial);const [holiday,setHoliday]=useState('');useEffect(()=>setForm({...initial,...(property?.settings||{})}),[property]);const save=async()=>{try{if(!property)throw new Error('Create your property profile first.');await saveOwnerProperty(user.uid,{id:property.id,settings:form});await refresh();toast.success('Settings saved.')}catch(e){toast.error(e.message)}};const toggle=(k)=>setForm((p)=>({...p,notifications:{...p.notifications,[k]:!p.notifications[k]}}));return <><PropertyPageHeader title="Settings" description="Control notifications, communication preferences, business hours, and holiday closures."/><div className="space-y-6"><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Notifications</h2><div className="mt-4 grid gap-3 sm:grid-cols-3">{Object.entries(form.notifications).map(([k,v])=><label key={k} className="flex items-center gap-3 rounded-2xl border border-default p-4 text-sm font-bold capitalize"><input type="checkbox" checked={v} onChange={()=>toggle(k)} className="size-4 accent-[#3F7D58]"/>{k}</label>)}</div><div className="mt-4 flex flex-wrap gap-5"><label className="flex items-center gap-2 text-sm font-bold"><input type="checkbox" checked={form.emailPreferences} onChange={(e)=>setForm((p)=>({...p,emailPreferences:e.target.checked}))}/>Email notifications</label><label className="flex items-center gap-2 text-sm font-bold"><input type="checkbox" checked={form.whatsappPreferences} onChange={(e)=>setForm((p)=>({...p,whatsappPreferences:e.target.checked}))}/>WhatsApp notifications</label></div></section><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Business hours</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Opening time<input type="time" value={form.businessHours.open} onChange={(e)=>setForm((p)=>({...p,businessHours:{...p.businessHours,open:e.target.value}}))} className="mt-2 w-full rounded-2xl border border-default px-4 py-3"/></label><label className="text-sm font-bold">Closing time<input type="time" value={form.businessHours.close} onChange={(e)=>setForm((p)=>({...p,businessHours:{...p.businessHours,close:e.target.value}}))} className="mt-2 w-full rounded-2xl border border-default px-4 py-3"/></label></div></section><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Holiday schedule</h2><div className="mt-4 flex gap-2"><input type="date" value={holiday} onChange={(e)=>setHoliday(e.target.value)} className="rounded-2xl border border-default px-4 py-3"/><button onClick={()=>{if(holiday&&!form.holidays.includes(holiday)){setForm((p)=>({...p,holidays:[...p.holidays,holiday]}));setHoliday('')}}} className="rounded-2xl bg-dark-surface px-5 py-3 font-bold text-white">Add holiday</button></div><div className="mt-4 flex flex-wrap gap-2">{form.holidays.map((d)=><button key={d} onClick={()=>setForm((p)=>({...p,holidays:p.holidays.filter((x)=>x!==d)}))} className="rounded-full bg-red-50 px-3 py-1 text-xs font-bold text-red-700">{d} ×</button>)}</div></section><div className="flex justify-end"><button onClick={save} className="rounded-2xl bg-dark-surface px-6 py-3 font-bold text-white">Save settings</button></div></div></>}
