import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { Loading } from '../../components/common/Loading';import { PageContainer } from '../../components/common/PageContainer';import { ReceiptViewer } from '../../components/booking/ReceiptViewer';import { useAuth } from '../../hooks/useAuth';import { getBooking } from '../../services/bookingService'
export default function ReceiptPage(){const {bookingId}=useParams();const {user,role}=useAuth();const [booking,setBooking]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{getBooking(bookingId).then(b=>{if(b&&(role==='admin'||b.guestId===user?.uid||b.ownerId===user?.uid))setBooking(b)}).finally(()=>setLoading(false))},[bookingId,role,user]);if(loading)return <Loading/>;return <PageContainer className="py-10">{booking?<ReceiptViewer booking={booking}/>:<h1 className="text-center text-3xl font-black">Receipt not found</h1>}</PageContainer>}
