import { useState } from 'react'
import { LogOut, Menu, Search, ShieldCheck, X } from 'lucide-react'
import { NavLink, Outlet } from 'react-router-dom'
import { ADMIN_NAVIGATION } from '../constants/admin'
import { useAuth } from '../hooks/useAuth'
const navClass=({isActive})=>`block rounded-xl px-3 py-2 text-sm font-semibold transition ${isActive?'bg-donroom-primary text-black':'text-muted hover:bg-black/5 hover:text-black'}`
export function AdminLayout(){const[open,setOpen]=useState(false);const{profile,logout}=useAuth();return <div className="min-h-screen bg-page"><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r border-default bg-white p-4 transition lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between"><NavLink to="/admin/dashboard" className="text-xl font-black">DONROOM <span className="text-donroom-success">Admin</span></NavLink><button className="rounded-lg p-2 lg:hidden" onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button></div><nav className="mt-6 max-h-[calc(100vh-8rem)] space-y-1 overflow-y-auto" aria-label="Admin navigation">{ADMIN_NAVIGATION.map(([label,path])=><NavLink key={path} to={`/admin/${path}`} onClick={()=>setOpen(false)} className={navClass}>{label}</NavLink>)}</nav></aside><div className="lg:pl-72"><header className="sticky top-0 z-40 flex h-16 items-center gap-3 border-b border-default bg-white/90 px-4 backdrop-blur lg:px-8"><button onClick={()=>setOpen(true)} className="rounded-xl p-2 lg:hidden" aria-label="Open menu"><Menu/></button><div className="relative hidden max-w-md flex-1 sm:block"><Search className="absolute left-3 top-2.5 h-4 w-4 text-subtle"/><input className="w-full rounded-xl border border-default bg-black/[.02] py-2 pl-9 pr-3" placeholder="Search administration" aria-label="Search administration"/></div><div className="ml-auto text-right"><p className="text-sm font-bold">{profile?.fullName||'Administrator'}</p><p className="text-xs text-subtle">Super administrator</p></div><ShieldCheck className="text-donroom-success"/><button onClick={logout} className="rounded-xl p-2 hover:bg-black/5" aria-label="Log out"><LogOut className="h-5 w-5"/></button></header><main className="p-4 lg:p-8"><Outlet/></main></div></div>}
