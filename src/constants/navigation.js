import { ROUTES } from './routes'
export const PUBLIC_NAVIGATION = Object.freeze([
  { label:'Home', to:ROUTES.HOME }, { label:'Properties', to:ROUTES.PROPERTIES }, { label:'Gallery', to:ROUTES.GALLERY }, { label:'About', to:ROUTES.ABOUT }, { label:'Contact', to:ROUTES.CONTACT }, { label:'FAQ', to:ROUTES.FAQ },
])
