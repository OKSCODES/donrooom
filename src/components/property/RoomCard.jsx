import { Copy, Edit3, Trash2, Users, BedDouble, Images } from 'lucide-react'
import { StatusBadge } from './StatusBadge'
export function RoomCard({room,onEdit,onDelete,onDuplicate,onImages}){return <article className="overflow-hidden rounded-3xl border border-default bg-white shadow-sm"><div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-donroom-success">Room {room.roomNumber}</p><h3 className="mt-1 text-xl font-black">{room.roomName}</h3></div><StatusBadge status={room.status}/></div><p className="mt-3 line-clamp-2 text-sm leading-6 text-muted">{room.description}</p><div className="mt-4 flex flex-wrap gap-3 text-sm text-black/65"><span className="inline-flex items-center gap-1"><Users size={16}/>{room.adults} adults, {room.children} children</span><span className="inline-flex items-center gap-1"><BedDouble size={16}/>{room.beds} beds</span></div><div className="mt-5 flex items-end justify-between"><p><span className="text-2xl font-black">₹{Number(room.pricePerNight||0).toLocaleString('en-IN')}</span><span className="text-xs text-muted"> / night</span></p><div className="flex gap-1"><button aria-label="Manage room images" onClick={()=>onImages(room)} className="rounded-xl p-2 hover:bg-black/5"><Images size={18}/></button><button aria-label="Edit room" onClick={()=>onEdit(room)} className="rounded-xl p-2 hover:bg-black/5"><Edit3 size={18}/></button><button aria-label="Duplicate room" onClick={()=>onDuplicate(room)} className="rounded-xl p-2 hover:bg-black/5"><Copy size={18}/></button><button aria-label="Delete room" onClick={()=>onDelete(room)} className="rounded-xl p-2 text-red-600 hover:bg-red-50"><Trash2 size={18}/></button></div></div></div></article>}
