import { useEffect } from 'react'; import { X } from 'lucide-react'; import { motion } from 'framer-motion'
export function Modal({open,title,onClose,children,size='max-w-3xl'}){useEffect(()=>{if(!open)return;const key=(e)=>e.key==='Escape'&&onClose();document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-[100] grid place-items-center bg-black/50 p-4" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={(e)=>e.target===e.currentTarget&&onClose()}><motion.div initial={{opacity:0,scale:.97}} animate={{opacity:1,scale:1}} className={`max-h-[92vh] w-full overflow-y-auto rounded-3xl bg-page shadow-2xl ${size}`}><header className="sticky top-0 z-10 flex items-center justify-between border-b border-default bg-page/95 px-6 py-4 backdrop-blur"><h2 id="modal-title" className="text-xl font-black">{title}</h2><button aria-label="Close dialog" onClick={onClose} className="rounded-xl p-2 hover:bg-black/5"><X/></button></header><div className="p-6">{children}</div></motion.div></div>}
