import { BedDouble, MapPin, Users } from 'lucide-react'
import { memo } from 'react'
import { Link } from 'react-router-dom'
import { Rating } from './Rating'
function Card({ property, view='grid' }) { return <article className={`group overflow-hidden rounded-[1.75rem] border border-default bg-white shadow-[0_18px_60px_rgba(45,45,45,.08)] transition duration-300 hover:-translate-y-1 hover:shadow-[0_24px_70px_rgba(45,45,45,.14)] ${view==='list'?'md:flex':''}`}><div className={`relative overflow-hidden ${view==='list'?'md:w-2/5':'aspect-[4/3]'}`}><img alt={property.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy" onError={(e)=>{e.currentTarget.src='https://images.unsplash.com/photo-1566073771259-6a8506099945?auto=format&fit=crop&w=900&q=70'}} src={property.image}/><span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1.5 text-xs font-bold backdrop-blur">{property.availableRooms} rooms available</span></div><div className="flex flex-1 flex-col p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-donroom-success">{property.type}</p><h3 className="mt-1 text-xl font-black">{property.name}</h3></div><Rating value={property.rating}/></div><p className="mt-2 flex items-center gap-1.5 text-sm text-muted"><MapPin size={15}/>{property.location}</p><div className="mt-4 flex flex-wrap gap-2">{property.amenities.slice(0,3).map((item)=><span className="rounded-full bg-cream px-3 py-1 text-xs font-semibold" key={item}>{item}</span>)}</div><div className="mt-5 flex items-end justify-between gap-4 border-t border-default pt-4"><div><p className="text-xs text-muted">From</p><p className="text-xl font-black">₹{property.price.toLocaleString()}<span className="text-sm font-medium text-muted"> / night</span></p></div><Link className="rounded-full bg-dark-surface px-5 py-2.5 text-sm font-bold text-white transition hover:bg-donroom-success focus-visible:ring-2 focus-visible:ring-[#3F7D58]" to={`/property/${property.id}`}>View details</Link></div><div className="sr-only"><BedDouble/> <Users/></div></div></article> }
export const PropertyCard = memo(Card)
