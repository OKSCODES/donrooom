import { MapPin } from 'lucide-react';import { Link } from 'react-router-dom';import { BookingStatusBadge } from './BookingStatusBadge';import { formatCurrency,formatDate } from '../../utils/booking'
export function BookingCard({booking}){return <article className="rounded-[1.75rem] border border-black/8 bg-white p-5 shadow-lg shadow-black/5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-donroom-success">{booking.bookingNumber}</p><h3 className="mt-1 text-xl font-black">{booking.propertyName}</h3><p className="mt-1 flex items-center gap-1 text-sm text-muted"><MapPin size={15}/>{booking.propertyLocation||'Sohra'}</p></div><BookingStatusBadge status={booking.status}/></div><div className="mt-5 grid gap-3 rounded-2xl bg-cream p-4 text-sm sm:grid-cols-3"><span><b>Room</b><br/>{booking.roomName}</span><span><b>Dates</b><br/>{formatDate(booking.checkIn)} – {formatDate(booking.checkOut)}</span><span><b>Total</b><br/>{formatCurrency(booking.grandTotal)}</span></div><Link className="mt-4 inline-flex font-black text-donroom-success" to={`/booking/${booking.id}`}>View booking →</Link></article>}
