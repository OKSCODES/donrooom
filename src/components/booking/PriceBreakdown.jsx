import { formatCurrency } from '../../utils/booking'
export function PriceBreakdown({pricing}){const rows=[['Room subtotal',pricing.roomSubtotal],['Discount',-pricing.discountAmount],['Tax (5%)',pricing.taxes],['Service fee (6%)',pricing.serviceFee]];return <div className="rounded-3xl border border-black/8 bg-white p-5"><h3 className="font-black">Price breakdown</h3><div className="mt-4 space-y-3">{rows.map(([label,value])=><div className="flex justify-between text-sm" key={label}><span className="text-muted">{label}</span><span className="font-bold">{formatCurrency(value)}</span></div>)}</div><div className="mt-4 flex justify-between border-t border-default pt-4 text-lg font-black"><span>Grand total</span><span>{formatCurrency(pricing.grandTotal)}</span></div></div>}
