export function AdminTable({rows,columns,actions,empty='No records found.'}){if(!rows.length)return <div className="rounded-3xl border border-dashed border-black/15 bg-white p-10 text-center text-muted">{empty}</div>;return <div className="overflow-x-auto rounded-3xl border border-default bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="bg-donroom-cream"><tr>{columns.map((c)=><th key={c.key} className="px-4 py-3 font-black">{c.label}</th>)}{actions&&<th className="px-4 py-3 font-black">Actions</th>}</tr></thead><tbody>{rows.map((row)=><tr key={row.id} className="border-t border-default align-top">{columns.map((c)=><td key={c.key} className="max-w-xs px-4 py-3 text-black/70">{c.render?c.render(row):String(row[c.key]??'—')}</td>)}{actions&&<td className="px-4 py-3">{actions(row)}</td>}</tr>)}</tbody></table></div>}
