import { CalendarDays, Download, Eye, XCircle } from 'lucide-react'
import { Link } from 'react-router-dom'
import { BookingStatusBadge } from '../booking/BookingStatusBadge'
const days=d=>Math.max(0,Math.ceil((new Date(d)-new Date())/86400000))
export default function GuestBookingCard({booking,onCancel,onReceipt}){return <article className="overflow-hidden rounded-3xl border border-default bg-white shadow-sm"><div className="grid sm:grid-cols-[180px_1fr]"><img className="h-44 w-full object-cover sm:h-full" loading="lazy" src={booking.propertyImage||'https://images.unsplash.com/photo-1566073771259-6a8506099945?auto=format&fit=crop&w=700&q=80'} alt=""/><div className="p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-widest text-donroom-success">{booking.bookingNumber}</p><h3 className="mt-1 text-xl font-black">{booking.propertyName||'DONROOM Property'}</h3><p className="text-sm text-muted">{booking.roomName||'Selected room'}</p></div><BookingStatusBadge status={booking.status}/></div><div className="mt-4 flex flex-wrap gap-4 text-sm"><span className="inline-flex items-center gap-2"><CalendarDays size={16}/>{booking.checkIn} → {booking.checkOut}</span><span>{days(booking.checkIn)} days remaining</span></div><div className="mt-5 flex flex-wrap gap-2"><Link to={`/guest/bookings/${booking.id}`} className="inline-flex items-center gap-2 rounded-full bg-dark-surface px-4 py-2 text-sm font-bold text-white"><Eye size={16}/>Details</Link><button onClick={()=>onReceipt(booking)} className="inline-flex items-center gap-2 rounded-full bg-primary-action px-4 py-2 text-sm font-bold"><Download size={16}/>Receipt</button>{['pending','confirmed'].includes(booking.status)&&<button onClick={()=>onCancel(booking)} className="inline-flex items-center gap-2 rounded-full border border-red-200 px-4 py-2 text-sm font-bold text-red-600"><XCircle size={16}/>Cancel</button>}</div></div></div></article>}
