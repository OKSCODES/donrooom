import { motion } from 'framer-motion'
export default function GuestDashboardCard({label,value,icon:Icon,help}){return <motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className="rounded-3xl border border-default bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm font-bold text-muted">{label}</p><p className="mt-2 text-3xl font-black">{value}</p>{help&&<p className="mt-1 text-xs text-subtle">{help}</p>}</div>{Icon&&<span className="rounded-2xl bg-primary-action p-3"><Icon size={21}/></span>}</div></motion.div>}
