import { MapPin } from 'lucide-react'
import { Link } from 'react-router-dom'
export function PropertyMap({properties}){return <div className="relative min-h-[620px] overflow-hidden rounded-[2rem] bg-[radial-gradient(circle_at_30%_20%,#dcebd9,transparent_24%),linear-gradient(135deg,#e8e3d5,#cfd9c6)]"><div className="absolute inset-0 opacity-30" style={{backgroundImage:'linear-gradient(#fff 1px,transparent 1px),linear-gradient(90deg,#fff 1px,transparent 1px)',backgroundSize:'42px 42px'}}/>{properties.map((p,i)=><div key={p.id} className="absolute" style={{left:`${12+(i*14)%72}%`,top:`${15+(i*17)%68}%`}}><Link to={`/property/${p.id}`} className="group relative block" aria-label={`View ${p.name}`}><span className="grid h-11 w-11 place-items-center rounded-full bg-donroom-success text-white shadow-xl"><MapPin size={22}/></span><span className="absolute left-1/2 top-12 z-10 hidden w-52 -translate-x-1/2 rounded-2xl bg-white p-3 shadow-xl group-hover:block group-focus:block"><strong className="block">{p.name}</strong><span className="text-sm text-muted">₹{p.price.toLocaleString()} · ★ {p.rating}</span></span></Link></div>)}</div>}
