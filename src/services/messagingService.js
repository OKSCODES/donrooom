export function buildWhatsAppBookingUrl(booking){ const phone=String(booking.ownerPhone||'').replace(/\D/g,''); const text=[`DONROOM Booking ${booking.bookingNumber}`,`Guest: ${booking.guestName}`,`Property: ${booking.propertyName}`,`Room: ${booking.roomName}`,`Dates: ${booking.checkIn} to ${booking.checkOut}`,`Total: ₹${booking.grandTotal}`,`Booking: ${window.location.origin}/booking/${booking.id}`].join('\n'); return `https://wa.me/${phone}?text=${encodeURIComponent(text)}` }
export const EMAIL_EVENT_TYPES=['booking_confirmation','booking_accepted','booking_rejected','booking_cancelled']
