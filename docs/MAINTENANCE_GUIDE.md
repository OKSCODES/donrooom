# Maintenance Guide

Review dependency advisories monthly and upgrade in a branch. Run CI before merging. Monitor Authentication failures, Firestore denials, Storage errors, web vitals, booking conversion, and error reference IDs. Review audit logs and pending property/review queues daily. Test backups quarterly. Rotate deployment credentials and service accounts at least annually. Publish rule/index changes before frontend code that requires them.
